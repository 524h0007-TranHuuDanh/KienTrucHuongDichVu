package com.tdtu.ibanking.gateway.config;

/**
 * Routes are declared only in application.yml. Keeping them in one place
 * avoids registering duplicate route definitions with identical IDs.
 */
public class GatewayConfig {
}
